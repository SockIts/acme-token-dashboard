# ACME Token Dashboard

Live shareholder services terminal for ACME token minting, tokenomics, portfolio views, market listings, program pages, and reports.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Production builds use the live ACME API at `https://acme.pics/api`.
